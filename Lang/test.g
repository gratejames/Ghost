#include "out.g"

int main() {
    // asm { BRK }
    // int arr[3];
    // arr;
    // asm { DBG R0 }
    // *arr;
    // asm { DBG R0 }
    // arr[0] = 0xabab;
    // *arr;
    // asm { DBG R0 }
    // arr[1] = 20;
    // if (arr[0] + arr[1] == 21) OUTLN("Array indexing OK");
    
    return 0;
}